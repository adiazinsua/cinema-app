import { environment } from '../../environments/environment';

class ApiRoutes {
  //   public login(): string {
  //     return `${environment.api}/users/login`;
  //   }
}

export const apiRoutes = new ApiRoutes();
